(trigger
    (tab lib))

(name [Load Library using “library”])

(only-in [source.r, source.rd.console])

(input nothing)

(output snippet)

(save nothing)

(script [echo -en 'library(${1:"${2:`"$TM_BUNDLE_SUPPORT"/bin/insert_package_name.sh`}"})${0:}'])