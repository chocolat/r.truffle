(trigger
    (key [[alt][shift][cmd]L]))

(name [Package Name…])

(only-in [source.r, source.rd.console])

(input nothing)

(output nothing)

(save nothing)

(script "$TM_BUNDLE_SUPPORT"/bin/insert_package_name.sh)